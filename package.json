{
  "name": "shop-assignment",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "common": "pnpm --filter common",
    "shop": "pnpm --filter shop",
    "svg-comp-gt": "pnpm --filter svg-component-generator"
  },
  "keywords": [],
  "author": "dfd1123",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "eslint": "^8"
  }
}
